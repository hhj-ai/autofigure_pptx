# Retrieval-Augmented Method Pipeline

The method retrieves supporting passages, encodes them with the query, routes the combined representation through a reasoning module, and produces an answer. A muted feedback signal updates the retriever after training.
